/** @type {import('next').NextConfig} */
const nextConfig = {
  serverOptions: {
    hostname: '127.0.0.1', // Change this to your desired IP
    port: 8000, // Change this to your desired port
  }
}

module.exports = nextConfig